<h1><%= @department.name %></h1>

<p>
   <h2>Professors:</h2>
   <ul id = "Professors">
	   <% @professors.each do |c| %>
	   <li>
	        <%= link_to c.name, {:controller => 'professor', :action => 'show', :id => c.id} %>
	   </li>
	</ul>
	<% end %>

   <h2>Courses:</h2>
   <ul id = "Courses">
	   <% @courses.each do |c| %>
	   <li>
	         <%= link_to c.name, {:controller => 'course', :action => 'show', :id => c.id}  %>
	        
	   </li>
	</ul>
	<% end %>

</p>

<hr />

