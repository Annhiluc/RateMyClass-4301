
<% if @schools.blank? %>
<p>There are not any schools currently in the system.</p>
<% else %>
<p>These are the current schools in our system</p>


   <% @schools.each do |c| %>
   <ul id = "Schools">
   <li>
        <%= link_to c.name, {:action => 'show', :id => c.school_id}, :style=>'color: #4c164f;' -%>
        
   </li>
</ul>

<% end %>

<% end %>